# Amazon-clone
Amazon clone by Mohammed. Built with React + TypeScript + Vite.

## Scripts
- `npm run dev` start local dev server
- `npm run build` build for production
- `npm run preview` preview production build
